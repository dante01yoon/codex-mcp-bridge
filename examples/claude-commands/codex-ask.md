Delegate the following question to Codex via the bridge and return the answer.

Call mcp__codex-bridge__consult_codex with:
- query: the user's question below
- directory: current working directory
- format: "text"
- timeout: 180
- sandbox: "read-only"

Return the Codex result directly, adding your own commentary only if the result needs clarification or correction.

$ARGUMENTS
